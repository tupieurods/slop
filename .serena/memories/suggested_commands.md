# Suggested Commands

## Build & Run
```
dotnet build src/SlopChat/SlopChat.csproj
dotnet run --project src/SlopChat/SlopChat.csproj
dotnet publish src/SlopChat/SlopChat.csproj
```

## Git (Windows)
```
git status
git log --oneline
git diff
git add <file>
git commit -m "message"
```

## File system (Windows bash/Git Bash)
```
ls
ls -la
cat <file>
find . -name "*.cs"
```

## .NET CLI
```
dotnet add package <PackageName>
dotnet restore
dotnet clean
```

## Testing (no tests yet)
No test project exists yet.
